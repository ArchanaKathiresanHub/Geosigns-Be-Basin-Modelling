# Make.qwt - contains definitions for Qwt 3rd party libraries
#
# depends on Qt
#

#
# Definitions for the base directory location for OPTIMIZED or DEBUG 
#   compiled include files and libraries
#
QWTDIR = $(QWT_HOME)/OPTIMIZED

#   Qwt definition
QWTINC = $(QWTDIR)/include
QWTLIB = $(QWTDIR)/lib

####### Include file definitions
CINCLUDE += -I$(QWTINC)
CXXINCLUDE += -I$(QWTINC)

####### Library definitions
CLIBS += -L$(QWTLIB) -lqwt 

# End 
