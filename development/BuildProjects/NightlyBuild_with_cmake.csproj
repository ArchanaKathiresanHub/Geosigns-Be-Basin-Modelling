<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Release</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">x64</Platform>
    <ProjectGuid>{C56F1DEC-42D3-46C6-9C94-8EA193E9C798}</ProjectGuid>
    <TargetFrameworkVersion>v3.5</TargetFrameworkVersion>
    <SccProjectName>SAK</SccProjectName>
    <SccLocalPath>SAK</SccLocalPath>
    <SccProvider>SAK</SccProvider>
    <SccAuxPath>SAK</SccAuxPath>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|x64' ">
    <OutputPath>x64\Release\</OutputPath>
    <CodeAnalysisRuleSet>AllRules.ruleset</CodeAnalysisRuleSet>
  </PropertyGroup>
  <PropertyGroup>
    
    <!--Paths as in the build server. The GNU/Linux blade used is Alfred's -->
    <CopyScript1>c:\opt\putty\PSCP.EXE -i "C:\opt\putty\private_id.ppk" "$(MSBuildProjectDirectory)\..\..\scripts\BatchBuild_with_cmake" "ksaho3@amsd2a-n-b01001:bin" </CopyScript1>
    <RemoteBuild>C:\opt\putty\PLINK.exe -batch -ssh -i "C:\opt\putty\private_id.ppk"  ksaho3@amsd2a-n-b01001 "/bin/sh -x ~/bin/BatchBuild_with_cmake" </RemoteBuild>
    
  </PropertyGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <Target Name="Build">
    <Message Text="TeamProject is $(TeamProject)"/>
    <Message Text="TeamFoundationServerUrl is $(TeamFoundationServerUrl)"/>
    <Message Text="BuildDefinitionName is $(BuildDefinitionName)"/>
    <Message Text="LinuxBuildRoot is $(LinuxBuildRoot)"/>
    <Message Text="BuildDefinitionID is $(BuildDefinitionID)"/>
    <Message Text="SourcePath is $(SourcePath)"/>
    <Message Text="Configuration is $(Configuration)"/>
    <Message Text="SourceGetVersion is $(SourceGetVersion)"/>
    <Message Text="IncrementalGet is $(IncrementalGet)"/>
    <Message Text="IncrementalBuild is $(IncrementalBuild)"/>
    <Message Text="OutDir is $(OutDir)"/>
    <Message Text="MSBuildProjectDirectory is $(MSBuildProjectDirectory)"/>
    <Message Text="Setup build"/>

    <Exec IgnoreExitCode="false" Command="$(CopyScript1)" />
    <Exec IgnoreExitCode="false" Command="$(RemoteBuild)" />
 </Target>
</Project>
